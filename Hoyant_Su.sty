\NeedsTeXFormat{LaTeX2e}[1994/06/01]
\ProvidesPackage{Hoyant_Su}[2022/08/10 Hoyant Su Package]

%% Language and font encodings
\RequirePackage[Chinese]{babel}
\RequirePackage{fontspec}
\RequirePackage[UTF8]{ctex}
\RequirePackage{float}
%% Sets page size and margins
\RequirePackage[a4paper,top=2cm,bbottom=2cm,left=2.5cm,right=2.5cm,marginparwidth=2cm]{geometry}
\RequirePackage{titlesec}  %目录
\RequirePackage{titletoc}  %目录
%% Useful packages
\RequirePackage{amsmath,amsthm,amssymb,amsfonts}
\RequirePackage{graphicx}
\RequirePackage[colorinlistoftodos]{todonotes}
\RequirePackage[colorlinks=true, allcolors=blue]{hyperref}
\RequirePackage{listings}%用以设置代码环境\stset{}
\RequirePackage{circuitikz}
\RequirePackage{subfigure}
\RequirePackage{booktabs}%三线
\RequirePackage{tikz}
\usetikzlibrary{graphs}
\RequirePackage{xcolor}
\usepackage{draftwatermark}%添加水印

\DraftwatermarkOptions{text=\textit{Experiments in Signals and Systems}}
\SetWatermarkScale{0.13}
\SetWatermarkAngle{27.33} 
\DraftwatermarkOptions{color={[RGB]{220,220,255}}}
\RequirePackage[<options>]{animate}
\RequirePackage{multimedia}
\RequirePackage{media9}
\RequirePackage{movie15}
\titlecontents{section}
              [1.5cm]
              {\bf\large}%
              {\contentslabel{2.5em}}%
              {}%
              {\titlerule*[0.5pc]{$\cdot$}\contentspage\hspace*{1.5cm}}%
\titlecontents{subsection}
              [2.5cm]
              {\bf \Small}%
              {\contentslabel{2.5em}}%
              {}%
              {\titlerule*[0.5pc]{$\cdot$}\contentspage\hspace*{1.5cm}}%  
\titlecontents{subsubsection}
              [3cm]
              {\bf \small}%
              {\contentslabel{2.5em}}%
              {}%
              {\titlerule*[0.5pc]{$\cdot$}\contentspage\hspace*{1.5cm}}%  
\lstset{ %
  language=MATLAB,                % the language of the code
  basicstyle=\footnotesize,           % the size of the fonts that are used for the code
  numbers=left,                   % where to put the line-numbers
  numberstyle=\tiny\color[rgb]{0.4,0.2,0.2},  % the style that is used for the line-numbers
  stepnumber=2,                   % the step between two line-numbers. If it's 1, each line 
                                  % will be numbered
  numbersep=12pt,                  % how far the line-numbers are from the code
  backgroundcolor=\color{white},      % choose the background color. You must add \RequirePackage{color}
  showspaces=false,               % show spaces adding particular underscores
  showstringspaces=false,         % underline spaces within strings
  showtabs=false,                 % show tabs within strings adding particular underscores
  %frame=single,                   % adds a frame around the code
  rulecolor=\color{black},        % if not set, the frame-color may be changed on line-breaks within not-black text (e.g. commens (green here))
  tabsize=2,                      % sets default tabsize to 2 spaces
  captionpos=b,                   % sets the caption-position to bottom
  breaklines=true,                % sets automatic line breaking
  breakatwhitespace=false,        % sets if automatic breaks should only happen at whitespace
  title=\lstname,                   % show the filename of files included with \lstinputlisting;
                                  % also try caption instead of title
  keywordstyle=\color[rgb]{0.9,0.5,0.5},          % keyword style
  commentstyle=\color[rgb]{0.4,0.3,0.9},       % comment style
  stringstyle=\color[rgb]{0.35,0.85,0.5},         % string literal style
  escapeinside={\%*}{*)},            % if you want to add LaTeX within your code
  morekeywords={*,...}               % if you want to add more keywords to the set
}
%图片插入快捷指令 ：\Fig{}{}{}{}
\newcommand{\Fig}[4]{
\begin{figure}[H]
    \centering
    \includegraphics[scale = #1]{#2}
    \caption{#3}
    \label{#4}
\end{figure}
}


%行间公式插入快捷指令：\Fuc{}
\newcommand{\Fuc}[1]{
    \begin{center}
        \begin{equation}
            \begin{aligned}
                    #1
            \end{aligned}
        \end{equation}
    \end{center}
}

\newcommand{\ma}[1]{\mathrm{#1}}


